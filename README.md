# mfe-demo-wp4
A sample repo that has WP4 pulling in WP5 Federated Modules. Still a WIP

To run the project, go into each of the main folders and run ```yarn && yarn dev```. 

Go to http://localhost:4000 for the WP4 version
Go to http://localhost:4001 for the normal WP5 version
